# Simple Result test

fn divide(a, b):
    if b == 0:
        return Err("division by zero")
    return Ok(a / b)

let r1 = divide(10, 2)
print("divide(10, 2):", r1)

let r2 = divide(10, 0)
print("divide(10, 0):", r2)

match r1:
    Ok(x) => print("Success:", x)
    Err(e) => print("Error:", e)
